package com.promptvidya.trustdesk;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class TrustDeskApplicationTests {

    @Test
    void contextLoads() {
        // Toolchain checkpoint: if this test passes, your JDK, Maven
        // wrapper, and Spring Boot setup are ready for the course.
    }
}
